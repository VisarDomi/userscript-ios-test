{
  "name": "userscript-ios-test",
  "version": "5",
  "description": "Shared iOS Safari test transport and controller for userscripts",
  "type": "module",
  "scripts": {
    "build": "node increase.js && node scripts/build-debugger.mjs",
    "manual:probe": "node scripts/manual-foreground-probe.mjs",
    "test": "node --test"
  },
  "exports": {
    "./controller": "./src/controller.mjs"
  },
  "bin": {
    "userscript-ios-test": "./bin/cli.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
